[project]
name = "ds-tutor"
version = "0.1.0"
description = "Interactive Data Science tutor helps to guide sklearn projects."
readme = "README.md"
authors = [
    { name = "smdbz", email = "github.commodore391@passmail.net" }
]
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.1",
]

[build-system]
requires = ["uv_build>=0.9.2,<0.10.0"]
build-backend = "uv_build"
